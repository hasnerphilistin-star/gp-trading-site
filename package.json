{
  "name": "gp-trading-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.1.6",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.5.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
